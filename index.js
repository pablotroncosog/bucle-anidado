let pronoun = ['the','our'];
let adj = ['great', 'big' ];
let noun = ['jogger','racoon']; 

for (let i = 0; i < pronoun.length; i++) {
    for (let ad = 0; ad < adj.length; ad++) { 
        for (let ac = 0; ac < noun.length; ac++) 
        { console.log(pronoun [i] + '' + adj [ad] + '' + noun [ac] + '.com')
  }        
 }
}

